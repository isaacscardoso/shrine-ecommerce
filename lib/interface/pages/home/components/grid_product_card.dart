import 'package:flutter/material.dart';

class GridProductCard extends StatelessWidget {
  const GridProductCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Grid Card Product works!');
  }
}
